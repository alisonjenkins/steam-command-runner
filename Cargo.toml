[package]
name = "steam-command-runner"
version = "0.2.0"
edition = "2021"
description = "A Steam compatibility tool and command wrapper for Linux gaming"
license = "MIT"
repository = "https://github.com/alisonjenkins/steam-command-runner"

[[bin]]
name = "steam-command-runner"
path = "src/bin/steam-command-runner.rs"

[dependencies]
# Serialization
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "1"

# CLI
clap = { version = "4", features = ["derive", "env"] }

# VDF file handling
keyvalues-serde = "0.2"

# HTTP client for Steam API
reqwest = { version = "0.13", default-features = false, features = ["json", "blocking", "rustls", "gzip"] }

# Path handling
dirs = "6"

# Logging
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }

# Shell command parsing
shlex = "2"

# Error handling
thiserror = "2"

# Raw USB HID (Ultimate Hacking Keyboard keymap switching)
hidapi = "2"

[dev-dependencies]
cucumber = "0.23"
futures = "0.3"
tempfile = "3"

[[test]]
name = "config"
harness = false

[[test]]
name = "update-steam"
harness = false
